# AutoCrate
AutoCrate is a Rust-powered CLI tool that automates project setup. It creates sequential Rust projects (gpt1, gpt2…), initializes git, and opens them in your editor. Future updates will add GitHub integration, templates, background mode, and a GUI for a smoother dev workflow.
